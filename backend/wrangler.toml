name = "anim-studio-api"
main = "src/index.js"
compatibility_date = "2024-12-01"

[vars]
# 环境变量通过 wrangler secret 设置，不写在这里
# GEMINI_API_KEY = "xxx"  ← 用 wrangler secret put GEMINI_API_KEY

# KV 存储：用户用量
[[kv_namespaces]]
binding = "USAGE_KV"
id = "YOUR_KV_NAMESPACE_ID"  # 部署前替换
